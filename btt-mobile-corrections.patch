diff --git a/.gitignore b/.gitignore
index 522f71b..51694b7 100644
--- a/.gitignore
+++ b/.gitignore
@@ -19,6 +19,10 @@ expo-env.d.ts
 npm-debug.*
 yarn-debug.*
 yarn-error.*
+_*.log
+_*.err
+_*.txt
+apk-build.log
 
 # OS
 .DS_Store
diff --git a/App.js b/App.js
index defa003..39e098b 100644
--- a/App.js
+++ b/App.js
@@ -4,7 +4,8 @@ import { createNativeStackNavigator } from '@react-navigation/native-stack';
 import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';
 import { StatusBar } from 'expo-status-bar';
 
-import { getSession } from './src/services/auth';
+import { getSession, clearSession } from './src/services/auth';
+import { setOnUnauthorized } from './src/services/api';
 
 import LoginScreen from './src/screens/LoginScreen';
 import RegisterScreen from './src/screens/RegisterScreen';
@@ -43,6 +44,10 @@ export default function App() {
   const [ready, setReady] = useState(false);
 
   React.useEffect(() => {
+    setOnUnauthorized(async () => {
+      await clearSession();
+      setUser(null);
+    });
     (async () => {
       const session = await getSession();
       setUser(session.user);
diff --git a/src/screens/LoginScreen.js b/src/screens/LoginScreen.js
index eac6324..86c063a 100644
--- a/src/screens/LoginScreen.js
+++ b/src/screens/LoginScreen.js
@@ -25,7 +25,6 @@ export default function LoginScreen({ navigation, onLogin }) {
       });
       await saveSession(data.token, data.user);
       onLogin(data.user);
-      navigation.replace('Home');
     } catch (err) {
       const msg = err.response?.data?.error || 'Connexion impossible';
       Alert.alert('Erreur', msg === 'OTP_REQUIRED' ? 'Un code OTP est requis' : msg);
diff --git a/src/screens/RegisterScreen.js b/src/screens/RegisterScreen.js
index 9f2f724..f67cc69 100644
--- a/src/screens/RegisterScreen.js
+++ b/src/screens/RegisterScreen.js
@@ -1,4 +1,4 @@
-﻿import React, { useState } from 'react';
+import React, { useState } from 'react';
 import {
   View, Text, TextInput, TouchableOpacity, StyleSheet, ActivityIndicator, Alert, ScrollView,
 } from 'react-native';
diff --git a/src/services/api.js b/src/services/api.js
index 78c1609..a4e8585 100644
--- a/src/services/api.js
+++ b/src/services/api.js
@@ -14,4 +14,21 @@ api.interceptors.request.use(async (config) => {
   return config;
 });
 
+// Callback enregistré par App.js pour déconnecter l'utilisateur
+// automatiquement quand le token n'est plus valide (401).
+let onUnauthorized = null;
+export const setOnUnauthorized = (callback) => {
+  onUnauthorized = callback;
+};
+
+api.interceptors.response.use(
+  (response) => response,
+  (error) => {
+    if (error.response?.status === 401 && onUnauthorized) {
+      onUnauthorized();
+    }
+    return Promise.reject(error);
+  }
+);
+
 export default api;
\ No newline at end of file